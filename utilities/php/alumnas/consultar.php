<?php 
	//Consulta base para alumnas select nomb_alum, nombre_grado from alumnas inner join grados where alumnas.grado_ingreso = grados.id_grado
	//Recordar realizar trigger para crear codigo de alumnas
	/* Tomar consideraciones para el trigger, año de ingreso a la institución + numero de orden en que se ingreso al sistema que sería el id 
		Si es 0 tiene que haber una forma de agregar los tres ceros antes de este el maximo de numeros será 9999 
	 */ 
?>